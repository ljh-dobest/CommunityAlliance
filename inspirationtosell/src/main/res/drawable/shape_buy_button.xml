<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item >
        <shape>
            <solid android:color="@color/nsdk_color_suspend" />
            <corners android:radius="@dimen/border_20"/>
            <padding
                android:left="@dimen/border_10"
                android:top="@dimen/border_10"
                android:right="@dimen/border_10"
                android:bottom="@dimen/border_10" />

            <!-- 描边，边框 -->
            <stroke
                android:width="1dp"
                android:color="@color/nsdk_color_suspend"
                />
        </shape>

    </item>
</selector>